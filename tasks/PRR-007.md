# PRR-007 — Compile real runtime imports and scopes

- Priority: **P0**
- Size: **L** (relative estimate, not a commitment)
- Dependencies: PRR-006

## Objective

Generate client-specific inclusion semantics: Claude @ imports, Codex/AGENTS scopes, Cursor MDC scopes, Copilot instruction variants and Windsurf AGENTS/rules.

## Acceptance criteria

- [ ] Claude fixture contains unquoted @ imports
- [ ] Nested agent/task rules compile to supported scoped files
- [ ] No adapter relies on generic Markdown links as automatic imports

## Verification

- [ ] Add or update automated fixtures.
- [ ] Run typecheck, tests and production build.
- [ ] Record evidence in the release report.

## Done when

The acceptance criteria pass and no unresolved P0 regression is introduced.
