# PRR-003 — Make builds reproducible

- Priority: **P0**
- Size: **M** (relative estimate, not a commitment)
- Dependencies: PRR-002

## Objective

Inject clock/build metadata, canonicalize ordering and separate deterministic core files from volatile build metadata.

## Acceptance criteria

- [ ] Two builds with the same canonical input are byte-identical in deterministic mode
- [ ] Stable ID includes complete canonical configuration
- [ ] ZIP timestamps are fixed or injected

## Verification

- [ ] Add or update automated fixtures.
- [ ] Run typecheck, tests and production build.
- [ ] Record evidence in the release report.

## Done when

The acceptance criteria pass and no unresolved P0 regression is introduced.
