# PRR-008 — Validate rendered Markdown and paths

- Priority: **P0**
- Size: **L** (relative estimate, not a commitment)
- Dependencies: PRR-002, PRR-007

## Objective

Parse actual Markdown/frontmatter and resolve links, imports, paths, duplicates, cycles, token budgets and adapter-specific constraints.

## Acceptance criteria

- [ ] Deleting any referenced file fails a fixture
- [ ] False-positive metadata-only links are eliminated
- [ ] Nested path traversal and duplicate paths are blocked

## Verification

- [ ] Add or update automated fixtures.
- [ ] Run typecheck, tests and production build.
- [ ] Record evidence in the release report.

## Done when

The acceptance criteria pass and no unresolved P0 regression is introduced.
