# PRR-014 — Add local workspace persistence and versioning

- Priority: **P1**
- Size: **L** (relative estimate, not a commitment)
- Dependencies: PRR-002, PRR-003

## Objective

Use IndexedDB for projects, autosave, import/export, history, manifest diff, rollback and rebuild.

## Acceptance criteria

- [ ] Reload restores project
- [ ] Export/import round trip is lossless
- [ ] User can compare and restore two versions

## Verification

- [ ] Add or update automated fixtures.
- [ ] Run typecheck, tests and production build.
- [ ] Record evidence in the release report.

## Done when

The acceptance criteria pass and no unresolved P0 regression is introduced.
